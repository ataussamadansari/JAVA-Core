package operators;

public class Operators {
    public static void main(String[] args) {
        //relational operators
            // compare two values and return a boolean result
                // == -> equal to
                // != -> not equal to
                // >= -> grater than equal to
                // <= -> less than equal to
                // < -> less than
                // > -> grater than

        /*//Example
        int a = 1;
        int b = 2;
        boolean c = b > a;
        System.out.println(c);*/

        //logical operators
            // && -> Logical AND
            // || -> Logical OR
            // ! -> Logical NOT

        //Example : marks -> 100/33 and age -> 18
        int marks = 36;
        int age = 18;

        System.out.println(marks >= 33 && age <= 19);
    }
}
